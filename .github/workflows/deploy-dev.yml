name: deploy-dev

# Trunk-dev deploy. Triggered on every push to `develop` (PRs land here via
# squash-merge). Deploys the canonical (suffix-less) bundle — all services
# and frontends discovered dynamically from the filesystem.
# (no `-dev` suffix — each env is its own workspace, so the namespace is
# scoped already). All service apps bind to their `production` Lakebase
# branch (the var default), which is the long-lived dev branch — separate
# from the per-PR `pr-<number>` branches owned by pr-validate.yml.
#
# Migrations run BEFORE bundle deploy: a deploy of a new app version that
# expects a column the DB doesn't yet have produces a brief outage; the
# reverse is fine because old code ignores the new column.

on:
  pull_request:
    branches: [develop]
    types: [closed]
  workflow_dispatch:

permissions:
  contents: read

# A second push to develop while we're still deploying queues behind us
# — we never want two `deploy-dev` runs racing each other.
concurrency:
  group: deploy-dev
  cancel-in-progress: false

env:
  DATABRICKS_CLI_VERSION: "1.2.1"
  DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST_DEV }}
  DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID }}
  DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET }}


jobs:
  # ---------------------------------------------------------------------------
  # Discover: enumerate services + frontends from the filesystem so every
  # downstream job uses a dynamic list rather than a hardcoded one.
  # ---------------------------------------------------------------------------
  discover:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    outputs:
      service_dirs: ${{ steps.disc.outputs.service_dirs }}
      frontend_dirs: ${{ steps.disc.outputs.frontend_dirs }}
      all_bundle_keys: ${{ steps.disc.outputs.all_bundle_keys }}
      all_app_names: ${{ steps.disc.outputs.all_app_names }}
      services_json: ${{ steps.disc.outputs.services_json }}
      frontends_json: ${{ steps.disc.outputs.frontends_json }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.11"
      - id: disc
        run: |
          OUT=$(./scripts/discover-projects.sh)
          echo "service_dirs=$(echo "$OUT" | jq -c '.service_dirs')" >> "$GITHUB_OUTPUT"
          echo "frontend_dirs=$(echo "$OUT" | jq -c '.frontend_dirs')" >> "$GITHUB_OUTPUT"
          echo "all_bundle_keys=$(echo "$OUT" | jq -c '.all_bundle_keys')" >> "$GITHUB_OUTPUT"
          echo "all_app_names=$(echo "$OUT" | jq -c '.all_app_names')" >> "$GITHUB_OUTPUT"
          echo "services_json=$(echo "$OUT" | jq -c '.services')" >> "$GITHUB_OUTPUT"
          echo "frontends_json=$(echo "$OUT" | jq -c '.frontends')" >> "$GITHUB_OUTPUT"

  test:
    needs: discover
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service: ${{ fromJSON(needs.discover.outputs.service_dirs) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
      - name: uv sync
        working-directory: services/${{ matrix.service }}
        run: uv sync --all-groups
      - name: Unit tests
        working-directory: services/${{ matrix.service }}
        run: uv run pytest -m 'not integration' -q

  test-frontends:
    needs: discover
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        frontend: ${{ fromJSON(needs.discover.outputs.frontend_dirs) }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
      - name: uv sync (BFF)
        working-directory: frontend/${{ matrix.frontend }}
        run: uv sync --all-groups
      - name: BFF unit tests
        working-directory: frontend/${{ matrix.frontend }}
        run: |
          rc=0
          uv run pytest -m 'not integration' -q || rc=$?
          if [[ $rc -eq 5 ]]; then
            echo "No tests collected — skipping"
            exit 0
          fi
          exit $rc

  deploy:
    needs: [discover, test, test-frontends]
    runs-on: ubuntu-latest
    environment:
      name: dev
      url: ${{ steps.portal-url.outputs.url }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6
        with:
          python-version: "3.11"
      - uses: astral-sh/setup-uv@37802adc94f370d6bfd71619e3f0bf239e1f3b78 # v7
      - uses: databricks/setup-cli@cb0efd3c46ddbb344dde5b171afdb7fefc3337ba # v1.4.0
        with:
          version: ${{ env.DATABRICKS_CLI_VERSION }}

      - name: Write hc-dev profile to ~/.databrickscfg
        env:
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST_DEV }}
          DATABRICKS_CLIENT_ID: ${{ secrets.DATABRICKS_CLIENT_ID }}
          DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_CLIENT_SECRET }}
        run: |
          mkdir -p "$HOME"
          cat > "$HOME/.databrickscfg" <<EOF
          [hc-dev]
          host = ${DATABRICKS_HOST}
          client_id = ${DATABRICKS_CLIENT_ID}
          client_secret = ${DATABRICKS_CLIENT_SECRET}
          EOF
          chmod 600 "$HOME/.databrickscfg"
          databricks current-user me -p hc-dev

      - name: Ensure Lakebase projects exist (idempotent)
        env:
          SERVICE_DIRS: ${{ needs.discover.outputs.service_dirs }}
        run: |
          for svc in $(echo "$SERVICE_DIRS" | jq -r '.[]'); do
            ./scripts/lakebase-project-up.sh "$svc" dev hc-dev
          done

      - name: Run migrations (production branches, all services)
        env:
          SERVICE_DIRS: ${{ needs.discover.outputs.service_dirs }}
        run: |
          set -euo pipefail
          export PGUSER=$(databricks current-user me -o json | python3 -c 'import json,sys; print(json.load(sys.stdin)["userName"])')
          for svc in $(echo "$SERVICE_DIRS" | jq -r '.[]'); do
            echo "→ Migrating $svc against production"
            cd "services/$svc"
            ENDPOINT="projects/$svc/branches/production/endpoints/primary"
            HOST=$(databricks postgres get-endpoint "$ENDPOINT" -o json | python3 -c 'import json,sys; print(json.load(sys.stdin)["status"]["hosts"]["host"])')
            export PGHOST="$HOST"
            export PGPORT=5432
            export PGDATABASE=databricks_postgres
            export PGSSLMODE=require
            export ENDPOINT_NAME="$ENDPOINT"
            uv sync --all-groups
            uv run alembic upgrade head
            cd "$GITHUB_WORKSPACE"
          done

      - name: Set up bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2

      - name: Cache bun dependencies
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v5
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('frontend/*/bun.lock') }}
          restore-keys: bun-${{ runner.os }}-

      - name: Install apx CLI
        run: |
          set -euo pipefail
          curl -fsSL https://databricks-solutions.github.io/apx/install.sh | sh -s -- --version v0.3.8
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Build all frontend UIs
        run: ./scripts/build-frontends.sh

      - name: bundle deploy
        run: databricks bundle deploy -t dev

      - name: bundle run (submit a new app deployment for each app, in parallel)
        env:
          ALL_BUNDLE_KEYS: ${{ needs.discover.outputs.all_bundle_keys }}
        run: |
          set -euo pipefail
          LOG_DIR=$(mktemp -d)
          mapfile -t KEYS < <(echo "$ALL_BUNDLE_KEYS" | jq -r '.[]')
          PIDS=()
          for KEY in "${KEYS[@]}"; do
            ( databricks bundle run -t dev "$KEY" ) > "$LOG_DIR/$KEY.log" 2>&1 &
            PID=$!
            PIDS+=("$PID")
            echo "→ kicked $KEY (pid=$PID)"
          done
          FAIL=0
          for i in "${!KEYS[@]}"; do
            KEY="${KEYS[$i]}"
            if wait "${PIDS[$i]}"; then
              echo "✓ $KEY"
              tail -3 "$LOG_DIR/$KEY.log" | sed 's/^/    /'
            else
              echo "::error::$KEY failed; full log:"
              cat "$LOG_DIR/$KEY.log"
              FAIL=1
            fi
          done
          rm -rf "$LOG_DIR"
          (( FAIL == 0 )) || exit 1

      - name: Wait for RUNNING state
        env:
          ALL_APP_NAMES: ${{ needs.discover.outputs.all_app_names }}
        run: |
          set -euo pipefail
          for app in $(echo "$ALL_APP_NAMES" | jq -r '.[]'); do
            for i in $(seq 1 20); do
              STATE=$(databricks apps get "$app" -p hc-dev -o json \
                | python3 -c 'import json,sys; print(json.load(sys.stdin).get("app_status",{}).get("state",""))')
              echo "  $app attempt $i/20: state=$STATE"
              if [[ "$STATE" == "RUNNING" ]]; then
                break
              fi
              if (( i == 20 )); then
                echo "::error::$app did not reach RUNNING within 5 minutes"
                exit 1
              fi
              sleep 15
            done
          done

      - name: Smoke test (honest /healthz check)
        env:
          SERVICES_JSON: ${{ needs.discover.outputs.services_json }}
          FRONTENDS_JSON: ${{ needs.discover.outputs.frontends_json }}
        run: |
          set -euo pipefail
          TOKEN=$(curl -sS --fail \
            -u "$DATABRICKS_CLIENT_ID:$DATABRICKS_CLIENT_SECRET" \
            --data-urlencode 'grant_type=client_credentials' \
            --data-urlencode 'scope=all-apis' \
            "$DATABRICKS_HOST/oidc/v1/token" \
            | python3 -c 'import json,sys; print(json.load(sys.stdin)["access_token"])')
          assert_healthz() {
            local app="$1" path="$2"
            local url body_file http_code body
            url=$(databricks apps get "$app" -p hc-dev -o json \
              | python3 -c 'import json,sys; print(json.load(sys.stdin).get("url",""))')
            if [[ -z "$url" ]]; then
              echo "::error::$app has no URL"
              return 1
            fi
            body_file=$(mktemp)
            http_code=$(curl -sS -o "$body_file" -w '%{http_code}' --max-time 30 \
              -H "Authorization: Bearer $TOKEN" \
              "$url$path")
            body=$(cat "$body_file"); rm -f "$body_file"
            if [[ "$http_code" != "200" ]]; then
              echo "::error::$app $path -> HTTP $http_code (expected 200)"
              echo "  body (first 300 chars): $(printf '%s' "$body" | head -c 300)"
              return 1
            fi
            if [[ "$body" != '{"ok":true}' ]]; then
              echo "::error::$app $path -> 200 but body mismatch:"
              echo "  body (first 300 chars): $(printf '%s' "$body" | head -c 300)"
              return 1
            fi
            echo "✓ $app $path -> 200 $body"
          }
          FAIL=0
          for row in $(echo "$SERVICES_JSON" | jq -c '.[]'); do
            app=$(echo "$row" | jq -r '.app_name')
            healthz=$(echo "$row" | jq -r '.healthz')
            assert_healthz "$app" "$healthz" || FAIL=1
          done
          for row in $(echo "$FRONTENDS_JSON" | jq -c '.[]'); do
            app=$(echo "$row" | jq -r '.app_name')
            healthz=$(echo "$row" | jq -r '.healthz')
            assert_healthz "$app" "$healthz" || FAIL=1
          done
          (( FAIL == 0 )) || exit 1

      - name: Resolve portal URL for environment link
        id: portal-url
        env:
          FRONTENDS_JSON: ${{ needs.discover.outputs.frontends_json }}
        run: |
          set -euo pipefail
          PORTAL=$(echo "$FRONTENDS_JSON" | jq -r '.[0].app_name')
          URL=$(databricks apps get "$PORTAL" -p hc-dev -o json \
            | python3 -c 'import json,sys; print(json.load(sys.stdin).get("url",""))')
          echo "url=$URL" >> "$GITHUB_OUTPUT"
          echo "Resolved $PORTAL URL: $URL"
